# ARD — Architecture Reference Documents

Mô tả kiến trúc **hiện tại** của backend MoonCraft. Sửa đè để luôn khớp code — không phải lịch sử (xem `../adr/`).

- **[architecture-core/](./architecture-core/)** — tổ chức tổng thể: layer, pattern, cross-cutting, dữ liệu & tích hợp, phần chưa làm.
- **[bounded-contexts/](./bounded-contexts/)** — từng phân hệ nghiệp vụ: làm gì, luồng, rule, edge case, bảng, endpoint.
- **[functional-requirements.md](./functional-requirements.md)** — danh sách chức năng đã triển khai, mã `FR-<CTX>-<NN>`.
